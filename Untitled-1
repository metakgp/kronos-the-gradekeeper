html for home
css for home
flask app for search
database

if request.method == "POST":
        code = request.form.get('coursecode')
        code = code + "wow"
        render_template('grim_reaper.html', output = code)
    else:
        render_template('grim_reaper.html', output = '')